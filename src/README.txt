Run instructions;

open a command line and within this folder and run:
  
  python AR2.py
  
make sure that all needed are installed or install them via pip. 

You also can build the python code to a standalone executable using py2exe. 
To do this you need to run:

  python setup.py py2exe
  
  
  